{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to use core Tauri commands and native file dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
